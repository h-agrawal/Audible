pipeline {
    agent any
    stages {
        stage('Deployment') {
            steps {
                bat '%userprofile%\\Downloads\\Jenkins\\BatchFiles\\Kill_Process.bat 4200'
                bat 'npm install --force'
                bat 'ng build'
            }
        }
    }
}